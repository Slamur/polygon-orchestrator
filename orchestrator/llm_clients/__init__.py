"""Конкретные реализации `orchestrator.llm_client.LLMClient` по вендорам.

Ничего отсюда не импортируется на верхнем уровне пакета
(`orchestrator/model_router.py` подтягивает `AnthropicClient` лениво, только
если явно не подменили клиент через `set_client`) — так модуль, который
использует только другого вендора, не тянет зависимости Anthropic SDK.
"""
